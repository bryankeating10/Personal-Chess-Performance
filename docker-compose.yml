version: "3.9"

services:
  chess:
    build: .
    container_name: chess-dev
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8888:8888"
    command: >
      jupyter lab --ip=0.0.0.0 --no-browser --allow-root